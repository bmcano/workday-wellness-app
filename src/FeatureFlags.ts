// This file contains flags so we can easily disable features we do not want to have appear in production or other demo enviornments
export default class FeatureFlags {
    ChatBotEnabled = false;
    LifetimeStatsEnabled = false;
}